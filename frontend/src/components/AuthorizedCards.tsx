import React from 'react';
import { Text } from 'react-native';

const AuthorizedCards = () => {
  return <Text className="text-xl">Placeholder for AuthorizedCards list</Text>;
};

export default AuthorizedCards;
